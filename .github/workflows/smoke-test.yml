name: smoke-test

on:
  # Kiểm tra production sau mỗi lần push và chạy định kỳ hằng ngày.
  # Lưu ý: services không ngủ (Serverless/App-Sleeping tắt) nên không cần keep-alive.
  # Workflow này chỉ đóng vai trò "uptime check" + kiểm tra nhanh sau mỗi deploy.
  push:
    branches: [main]
  schedule:
    - cron: "17 4 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app (SPA)
        run: |
          curl -sS -o /dev/null -w "app %{http_code} in %{time_total}s\n" \
            https://marvelous-creation-production-5d43.up.railway.app/
      - name: Ping API health
        run: |
          curl -sS -o /dev/null -w "api %{http_code} in %{time_total}s\n" \
            https://marvelous-creation-production-5d43.up.railway.app/api/v1/health
